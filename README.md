#  React Voting App

A beginner-friendly React app with one parent and two child components. Each child has a button that updates its text using state managed by the parent component.

## How to Run This App

### Clone the Repository

Open a terminal and run:

```bash
git clone https://github.com/UniqueMc/react-voting-app.git
cd react-voting-app

npm install
npm start
